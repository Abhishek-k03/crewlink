/** Shared control styling, so a palette change lands in one place rather than five. */
export const fieldClasses =
  'rounded-md border border-line bg-surface px-3 py-2 text-sm text-ink outline-none transition-colors focus:border-accent';

export const errorTextClasses = 'text-xs font-normal text-danger';
